{% extends 'base.html.twig' %}

{% block title %}Joke{% endblock %}

{% block body %}
    <h1>Joke</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joke.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ joke.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ joke.lastname }}</td>
            </tr>
            <tr>
                <th>Quote</th>
                <td>{{ joke.quote }}</td>
            </tr>
            <tr>
                <th>Funny</th>
                <td>{{ joke.funny }}</td>
            </tr>
            <tr>
                <th>Lousy</th>
                <td>{{ joke.lousy }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ joke.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('joke_index') }}">back to list</a>

    <a href="{{ path('joke_edit', {'id': joke.id}) }}">edit</a>

    {{ include('joke/_delete_form.html.twig') }}
{% endblock %}
